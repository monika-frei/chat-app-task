import React from "react";
import { StyleSheet, View, Text, FlatList, Dimensions } from "react-native";

const Chat = ({ number }) => {
  return <Text>Hello in chat number {number}</Text>;
};

export default Chat;
